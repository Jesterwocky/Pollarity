const React = require('react');
const ReactDOM = require('react-dom');
const hashHistory = require('react-router').hashHistory;

const SurveyReport = React.createClass({

  render() {
    return(
      <p>Survey results will be displayed</p>
    );
  }
});
